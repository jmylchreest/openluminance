# hardware/

KiCad projects for the tile PCB and the master/bus-driver board. Empty until the bus design is frozen — see [`docs/architecture.md`](../docs/architecture.md).

- `tile/` — per-tile PCB: CH32V003, LED cluster, edge connectors, polyfuse.
- `master/` — ESP32 master or bus-driver bridge.
