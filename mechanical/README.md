# mechanical/

Parametric tile enclosures, diffusers, and alignment features.

- `openscad/` — source `.scad` files. Uses libraries from [`thirdparty/`](../thirdparty/README.md).
- `stl/` — canonical exported STLs checked in for the current release. Iterative exports are gitignored.

Not yet implemented. First model to draw: the tile body with edge-connector pockets and magnet / alignment-dowel placements. Target print material is PETG.
